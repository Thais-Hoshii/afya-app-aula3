import React from 'react';

// import { Container } from './styles';

const NavBar: React.FC = () => {
    return (
        <nav>
            <h1>Topo</h1>
        </nav>
    );
}

export default NavBar;